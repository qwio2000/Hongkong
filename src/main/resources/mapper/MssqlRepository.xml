<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeiglobal.hk.repository.MssqlRepository">
	<select id="selectTest" resultType="int">
		SELECT 1
	</select>
	<select id="getPwdHash" resultType="String" parameterType="String">
		select pwd from jeiglobal..logininfo where sabun = #{empKey}
	</select>
</mapper>